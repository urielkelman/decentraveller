export type UserElementResponse = {
    owner: string;
    nickname: string;
    country: string;
    interest: string;
    createdAt: string;
};

export type UserResponse = {
    UserElementResponse;
};
