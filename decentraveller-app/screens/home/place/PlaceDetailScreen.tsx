import { View, Text } from 'react-native';

export type PlaceDetailScreen = {
    Place: undefined;
};

const PlaceDetailScreen = () => {
    return (
        <View>
            <Text>Place detail</Text>
        </View>
    );
};

export default PlaceDetailScreen;
