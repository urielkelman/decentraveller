name: Web API tests
run-name: ${{ github.actor }} 
on:
  push:
    paths:
      - descentraveller-api
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - run: docker-compose -f descentraveller-api/docker-compose.yml run tests